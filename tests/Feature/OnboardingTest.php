<?php

use Illuminate\Foundation\Testing\RefreshDatabase;

uses(RefreshDatabase::class);

it('would be a disaster if customer is not notified of completed template setup', function () {});

it('would be a disaster if customer cannot use email magic link', function () {});

it('would be a disaster if customer does not see project index', function () {});

//todo more
